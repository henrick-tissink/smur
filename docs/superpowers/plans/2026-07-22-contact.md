# Phase 5 — `/contact` page (faithful-fluid) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Rebuild the `/contact` page (sage hero → cream inquiry form → brown FAQ) faithful-fluid and swap the live `app/contact/page.tsx` off the zoom build, **without changing the working email flow** (`useContactSubmit` + `/api/contact` stay untouched), preserving every animation, and moving to the new nav.

**Architecture:** Three stacked sections. The **Hero** is pixel-composed → an aspect-ratio stage (`%`/`cqw`, `overflow: visible` so the LAVABO carousel keeps its intentional 79px overlap into the form). The **Form** and **FAQ** are already flow (the form is an internal `flex-col`; the FAQ grows with the accordion) → real flow sections with `max-w` containers, fluid gutters (`--gutter`/`--space-section`), and **readable px text** (17/15px — NOT cqw, so form fields stay legible). No `zoom`, no `transform:scale`.

**Tech Stack:** Next.js 16, React 19, Tailwind v4, `motion/react`, Vitest + RTL. Reuses `Reveal`, `TitleMask`, `BouncingArrow`, `Chevron`, **`useContactSubmit` (unchanged)**, `content/contact.ts`, new `components/navigation/*`.

**Phase context:** Plan 5 (spec: `docs/superpowers/specs/2026-07-20-faithful-fluid-rearchitecture-design.md`). Follows `/work` (Phase 4). Legacy `components/contact/page.tsx` + `components/mobile/contact-page.tsx` stay until the swap/cleanup tasks. `components/contact/use-contact-submit.ts` and `app/api/contact/*` are NEVER modified.

## Global Constraints

- **Runtime:** Node 22, pnpm 10.33.4. If missing: `export PATH="$HOME/.nvm/versions/node/v22.23.1/bin:$PATH"`.
- **DO NOT TOUCH the email flow:** reuse `useContactSubmit` (`components/contact/use-contact-submit.ts`) exactly; never modify it or `app/api/contact/*`. Preserve EVERY field `name`/`id` verbatim: `firstName`, `lastName`, `email` (`type=email required`), `business`, `interests` (checkbox group — ALL options share `name="interests"` so multiple selections POST as an array), `brandAbout`, `deadline`, `details` (textarea), `source`. Preserve the checkbox custom style, the textarea local-state overlay-placeholder pattern, the submit button state machine (`idle→sending→sent|error`, disabled on sending/sent, labels `SAVE & SEND`/`SENDING…`/`SENT`), and the `role="alert"` error line with the `mailto:hello@smur-world.com` link. Copy is verbatim from `content/contact.ts` (including the intentional typos "Identiy"/"adress").
- **PRESERVE ALL ANIMATIONS:** Reveal (delays: hero title 0 / body 0.08 / left img 0.12 / carousel 0.16; form whole 0; FAQ eyebrow 0 / title 0.06); `BouncingArrow` down cue; `LavaboCarousel` (desktop hero, 1400ms/220ms crossfade, preload frame 0); FAQ accordion (`max-h`/`opacity` 500ms + `Chevron` rotate); the "my work :)" `BouncingUnionArrow` (motion.span, x `[0,8,0]`, **1.0s desktop / 1.4s mobile**, reduced-motion aware); submit-arrow `group-hover:translate-x-1`; collage `hover:scale-[1.01]`; socials `hover:opacity-70`. Port motion unchanged.
- **Faithful + fluid, no `zoom`/`transform:scale`:** Hero = aspect-stage (`overflow:visible`); Form + FAQ = flow with `max-w` + `--gutter`/`--space-section`, px text.
- **New nav** on the route (`@/components/navigation`), `scheme="light"` (over the sage hero). MobileMenu already rewrites `/#x→/#m-x` (cross-page — fine).
- **Do NOT touch** the case-study routes/components or legacy nav (still used elsewhere).
- **Path alias:** `@/*` → repo root. TDD + frequent commits.

## Source values (from `components/contact/page.tsx`, `components/mobile/contact-page.tsx`, `content/contact.ts`)

- Desktop 1440×3093 (Hero h812 sage `#bbc2b5`/cream, `light`; Form h1227 cream `#f5f1ec`/accent, `dark`; FAQ flow brown `#906553`/cream, `light`). Mobile 393×2683 (Hero h866; Form h946; FAQ flow). See the inventory for exact absolute child coords (title/body/images/arrow/carousel per section).
- Title SVGs: `tell-me-about.svg` (hero, `as={1}`), `questions.svg` (FAQ, `as={2}`). Arrow mask `cta-union.svg`. Hero images `contact/hero-left.png` + `hero-right-{1,2,3}.png` (carousel, desktop only). FAQ strip `contact/faq-strip/{crisp,interstellar,kokop,taf}.png` (each links to its `/work/<slug>`).

---

### Task 1: Desktop `ContactHero`
**Files:** Create `components/sections/contact-hero.tsx` + test.
**Port** the `ContactHero` block from legacy `components/contact/page.tsx` (READ IT + the inventory) to an aspect-ratio stage: `<section data-nav-scheme="light">` bg sage `var(--color-band)` (#bbc2b5), cream text; stage `mx-auto w-full max-w-[1440px]`, `aspectRatio:"1440 / 812"`, `containerType:"inline-size"`, `overflow: visible` (the LAVABO carousel overflows the band ~79px, `z-10` — keep that), `data-contact-hero-stage`. Children → `%`/`cqw`: title `TitleMask tell-me-about.svg` (`as={1}`) at left505/top237, body 17px→cqw at left422/top408/w596, MANUFAKTURA `hero-left.png` at left0/top244 (258×356), the `LavaboCarousel` (port the local component — 3 frames `hero-right-{1,2,3}.png`, `useState`+`useEffect` `setInterval` 1400ms, `transition-opacity duration-[220ms]`, preload frame0) at left1066/top642 (374×249), `BouncingArrow direction="down" size={cqw(72)}` at left684/top642 — arrow + carousel are DIRECT children of the stage (containing-block rule from Phase 4). Reveal delays 0/.08/.12/.16.
- [ ] Test: renders `section` (nav-scheme light) + level-1 heading + body text (/thoughtful investment/); stage aspect 1440/812 + container-type inline-size. Red→green. Full `pnpm test`, `pnpm build`. Commit `feat(sections): fluid desktop ContactHero (aspect stage, LavaboCarousel preserved)`.

### Task 2: Desktop `ContactForm` (⚠️ email flow — highest risk)
**Files:** Create `components/sections/contact-form.tsx` + test.
**Port** the `ContactForm` block (+ its inline `FieldText`/`FieldTextarea`/`CheckboxGroup` field renderers) from legacy to a FLOW section: `<section data-nav-scheme="dark">` bg cream `var(--color-page)`; a centered form container `mx-auto` `max-w-[596px]` `w-full`, padding-x `var(--gutter)`, padding-y `var(--space-section)`. Keep the form EXACTLY: `const {state,submit}=useContactSubmit()` (import from `@/components/contact/use-contact-submit` — UNCHANGED), `<form onSubmit={e=>{e.preventDefault();void submit(e.currentTarget)}}>` `flex flex-col gap-[44px]`; row1 First+Last `flex gap-[10px]`; `contactForm.fields.slice(2).map` switching on `kind`; the field sub-components with their exact structure (sr-only label, placeholder=label+helper, underline `border-b-[2.113px] border-accent/50 focus:border-accent`, `text-[17px]`, `name={id}`, email `type=email required`; textarea overlay-placeholder local state, `h-[135px]`; checkbox group `<fieldset>`/`<legend>`, custom `appearance-none ... checked:bg-accent`, shared `name="interests"`); the submit button (state-driven classes, disabled logic, label, `cta-union.svg` mask arrow `group-hover:translate-x-1`); the `role="alert"` error line. Whole form in one `<Reveal>`. **Text stays px (17px), NOT cqw** (readable form). No `zoom`.
- [ ] Test (RTL): renders the form; the email input has `name="email"` `type="email"` `required`; every interest checkbox has `name="interests"`; the submit button shows `SAVE & SEND`; typing + submitting calls `fetch` (mock `global.fetch` → assert it POSTs to `/api/contact` with the field values, incl. `interests` as multiple). This test guards the email wiring. Red→green. Full `pnpm test`, `pnpm build`. Commit `feat(sections): desktop ContactForm (email flow + field names preserved, fluid section)`.

### Task 3: Desktop `ContactFAQ`
**Files:** Create `components/sections/contact-faq.tsx` + test.
**Port** the `ContactFAQ` block (+ inline `FAQRow` accordion + `BouncingUnionArrow`) to a FLOW section: `<section data-nav-scheme="light">` bg brown `var(--color-about)` (#906553), cream text, padding `var(--space-section)`/`var(--gutter)`; eyebrow ("I answered your") + `TitleMask questions.svg` (`as={2}`); the 4-item accordion (`contactFAQItems`, `FAQRow` = `useState(open)`, answer panel `overflow-hidden transition-[max-height,opacity] duration-500 ease-out` open `max-h-[600px] opacity-100`, `whitespace-pre-line`, `Chevron open width={22}`); the work collage — a `<Link href="/work">` strip of the 4 `contactFAQ.workThumbs` (`hover:scale-[1.01]`) — wait, the thumbs each have their own `href` in content but the desktop wraps the whole strip in one `/work` link per inventory; follow the legacy exactly; the "my work :)" `BouncingUnionArrow` (motion.span x`[0,8,0]` **1.0s**, cta-union mask); socials `INSTAGRAM / PINTEREST` (`instagramUrl`/`pinterestUrl`, `hover:opacity-70`, target=_blank). Make the collage/rows responsive (flow, `max-w` + fluid gutter). Reveal delays 0/.06.
- [ ] Test: renders the FAQ section (nav-scheme light) + heading; all 4 questions render; clicking a question toggles its `aria-expanded`; the socials links + the `/work` collage link present. Red→green. Full `pnpm test`, `pnpm build`. Commit `feat(sections): desktop ContactFAQ (accordion + collage, fluid)`.

### Task 4: Mobile `ContactHero` + `ContactForm`
**Files:** Create `components/sections/mobile-contact-hero.tsx`, `mobile-contact-form.tsx` + tests.
**Port** the mobile `Hero` + `Form` blocks from `components/mobile/contact-page.tsx`. Mobile Hero = aspect-stage `393/866` (`overflow:visible`), NO LAVABO carousel (only the MANUFAKTURA left thumb), arrow centered top545 (direct child of stage), title/body cqw (body 15px). Mobile Form = flow section, the mobile field components (`text-[15px]`, textarea `h-[110px]`), the SAME `useContactSubmit` wiring + field names + checkbox array + submit states + error line (identical to desktop, mobile sizes). Row1 hardcodes firstName/lastName (per legacy). One `<Reveal>` around the form. px text.
- [ ] Tests: mobile-contact-hero (stage 393/866, heading, body); mobile-contact-form (email input name/type/required; interests checkboxes name; SAVE & SEND; fetch POST on submit). Red→green. Full `pnpm test`, `pnpm build`. Commit `feat(sections): mobile ContactHero + ContactForm (email flow preserved)`.

### Task 5: Mobile `ContactFAQ`
**Files:** Create `components/sections/mobile-contact-faq.tsx` + test.
**Port** the mobile `FAQ` block: flow section brown, centered eyebrow+title, the accordion (`Chevron width={16}`, 15px), the mobile collage — a `mt-72 flex` row with the "my work :)" `BouncingUnionArrow` (**1.4s** mobile) on the LEFT and a **2×2 pinwheel** collage (crisp square 108 + interstellar wide flex-1 / kokop wide flex-1 + taf square 108, each ~120 tall) on the right — responsive; socials. `m-`-prefixed ids only if it has anchor targets (FAQ likely has none — check; if it needs a section id, no anchor points here, so a plain section is fine — no `m-` needed unless the nav links to it, which it doesn't). Reveal delays 0/.06.
- [ ] Test: renders + 4 questions + accordion toggle + socials/collage links. Red→green. Full `pnpm test`, `pnpm build`. Commit `feat(sections): mobile ContactFAQ (accordion + pinwheel collage, fluid)`.

### Task 6: Swap `app/contact/page.tsx` (live) + verify
**Files:** Rewrite `app/contact/page.tsx`.
Render both trees CSS-toggled, no-zoom, new nav (`scheme="light"`), preserve `metadata`:
```tsx
<>
  <div className="md:hidden"><MobileNav scheme="light" /><main><MobileContactHero/><MobileContactForm/><MobileContactFAQ/></main></div>
  <div className="hidden md:block"><Nav scheme="light" /><main><ContactHero/><ContactForm/><ContactFAQ/></main></div>
</>
```
(imports from `@/components/navigation/*` + `@/components/sections/*`; verify exact export names). Preserve the existing `metadata` (title "Let's Work Together — SMUR" + description). Mobile Form/FAQ may need small inter-section spacing (check legacy mobile gaps).
- [ ] Test: renders; both a desktop hero heading + a mobile one; the page contains a `name="email"` input and the SAVE & SEND button (form wired). `pnpm test`, `pnpm build` (all routes incl `/work/*` compile). Commit `feat(contact): swap live /contact to faithful-fluid (no zoom, new nav)`.
- [ ] **(controller visual + FUNCTIONAL check — REQUIRED before merge)** `pnpm dev` → `/contact`: desktop 1440 + ~800–1300 + mobile ≤767 — sections render, hero LAVABO carousel crossfades + overlaps the form band, arrow bounces, FAQ accordion expands, collage hovers, no overflow; **AND submit the form** (or at least confirm the POST to `/api/contact` fires with all fields incl multiple `interests`) — the email flow must still work.

### Task 7: Retire legacy contact components
- [ ] `grep` that `components/contact/page.tsx` + `components/mobile/contact-page.tsx` have zero importers (route now uses `components/sections/*`), then delete them. **KEEP** `components/contact/use-contact-submit.ts` (still imported by the new form sections) and `app/api/contact/*`. `pnpm test` + `pnpm build` green. Commit `chore(contact): retire legacy contact page components`.

---

## Self-Review (completed by plan author)
- **Spec coverage:** hero (aspect-stage) + form (flow) + FAQ (flow), desktop → Tasks 1–3, mobile → Tasks 4–5; live swap → Task 6; cleanup → Task 7. Email flow preserved — `useContactSubmit`/`/api/contact` untouched, field names/checkbox-array/submit-states verbatim (Tasks 2/4 + guarded by fetch-POST tests). Animations reused unchanged. Faithful-fluid no-zoom. New nav. Content verbatim (incl. typos).
- **Placeholder scan:** section tasks are port-from-legacy with explicit fluid rules (aspect-stage for hero, flow for form/FAQ) + tests; the form tasks additionally get a fetch-POST test guarding the wiring. No vague placeholders.
- **Type consistency:** `useContactSubmit` `{state,submit}` used identically in desktop/mobile forms; `ContactField` union from content drives field rendering; `BouncingArrow size` cqw (Phase-4 string support). New nav imports resolve to `@/components/navigation/*`.
- **Scope/risk:** email flow untouched (highest risk mitigated by reuse + tests); case-study routes + legacy nav untouched; Task 6 is a live swap (isolated to `/contact`) with a REQUIRED functional+visual check (submit the form) before merge.
